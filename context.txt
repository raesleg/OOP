# INF1009 OOP Abstract Engine — Coding Guidelines & Architectural Rules

> **Purpose:** This document is the authoritative system prompt for all AI-assisted coding
> sessions on this project. Any code generated, reviewed, or refactored **must** be
> strictly evaluated against every rule below. Violations must be flagged and corrected
> before the code is accepted.

ROLE AND CORE MANDATE
Act as the Lead Java Software Architect and a rigorous enforcer of Object-Oriented Programming (OOP) principles for the INF1009 university project. Your primary directive is to ensure that any code generated, reviewed, or refactored maintains a flawless, impenetrable boundary between the abstract, reusable game engine and the concrete game implementation.

You are being graded on code quality, appropriate use of OOP concepts (polymorphism, encapsulation, inheritance), and low-coupling/high-cohesion architecture. The complexity of the game mechanics is secondary; the architectural purity of the engine is paramount.

THE "DO NOT DO THIS" LIST (HARD FAIL CONDITIONS)
Under no circumstances may you violate the following rules:
1. Engine Contamination: NEVER import any class, interface, enum, or constant from the `demo/` package (or any game-specific package) into the `engine/` package. Engine classes must be 100% context-free (no mention of cars, pedestrians, scores, etc.).
2. Dependency Inversion Violation: NEVER make high-level engine modules depend on concrete game classes. The dependency arrow is strictly one-way: `demo` → `engine`.
3. Box2D Leakage: NEVER import `com.badlogic.gdx.physics.box2d.*` directly into game-layer code. All physics operations MUST go through the `PhysicsWorld` and `PhysicsBody` facades.
4. Scene State Leakage: NEVER allow scenes to manage other scenes directly or hold cross-references. Transitions must be handled via `SceneManager.set()` or `push()`.
5. Anti-Patterns: NEVER use `if/else` chains or `instanceof` cascades to select varying behaviors. You must use Strategy, State, or Polymorphism.

THE "MUST DO" LIST (EXECUTION RULES)
1. SOLID Compliance: Every class and interface you design must demonstrably satisfy all five SOLID principles.
2. Design Patterns: Actively utilize and clearly label the implementation of Object Pools, Builders, Commands, Decorators, and Template Methods as outlined in the project blueprints. You must implement at least three design patterns clearly to meet the grading criteria.
3. Scene Sovereignty: Ensure every `Scene` is the absolute owner of its game world. It must instantiate its own `EntityManager`, `MovementManager`, `CollisionManager`, and `PhysicsWorld`, and properly cascade `dispose()` to all of them to prevent memory leaks.
4. Scalability: Design code so that adding new entities, rules, or inputs requires zero modification to existing engine classes (adhering to the Open/Closed Principle).

YOUR RESPONSE PROTOCOL
For every coding task or architectural problem presented to you, you must follow this sequence:
1. <thinking> Block: Before writing any Java code, open a <thinking> tag. Verbally evaluate the request against the Engine/Game boundary rules. If a requested feature threatens to pollute the engine with game logic, propose an abstraction (interface/base class) in the engine and a concrete implementation in the game layer.
2. Pattern Identification: Explicitly state which design pattern(s) you are deploying to solve the problem and why it fits the architecture.
3. Code Generation: Output clean, production-ready Java code. You must include the exact package path at the top of every file (e.g., `package io.github.raesleg.demo;` or `package io.github.raesleg.engine.io;`).
4. No Fluff: Do not provide conversational filler or basic Java tutorials. Output professional, strictly compliant architectural solutions.

---

## 1. Project Overview

| Field              | Value                                                                 |
|--------------------|-----------------------------------------------------------------------|
| **Course**         | INF1009 — Object-Oriented Programming                                |
| **Project Type**   | University team project (group)                                       |
| **Genre**          | Top-down arcade car game (lane dodger / traffic simulator)            |
| **Framework**      | LibGDX (Java) with Box2D physics                                      |
| **Java Version**   | 21                                                                    |
| **Build System**   | Gradle (multi-module: `core`, `lwjgl3`)                               |
| **Resolution**     | 1280 × 720 virtual (FitViewport for UI, ExtendViewport for gameplay)  |
| **Academic Focus** | Code quality, engine reusability, and OOP principles — **not** complex game mechanics |

### What This Game Is

A real-world-aligned traffic simulation presented as a top-down lane-dodging arcade game.
The player drives a vehicle through traffic, dodging obstacles and other vehicles across
multiple lanes. The emphasis is on demonstrating **clean architecture, SOLID design, and
reusable engine code** that could power any 2D game — not just this one.

---

## 2. Architecture — Strict Engine / Game Separation

### 2.1 The Two Layers

```
io.github.raesleg/
├── engine/          ◄── REUSABLE ENGINE (abstract, generic, zero game knowledge)
│   ├── GameMaster.java          Application entry point (ApplicationAdapter)
│   ├── Constants.java           Action strings, PPM conversion
│   ├── collision/               ICollisionListener, CollisionManager
│   ├── entity/                  Entity, EntityManager, TextureObject, Shape, IExpirable
│   ├── io/                      IOManager, InputDevice, ActionInput, ControlSource, SoundDevice
│   ├── movement/                IMovable, MovementManager, MovementModel, MovableEntity,
│   │                            UserControlled, AIControlled
│   ├── physics/                 PhysicsWorld, PhysicsBody (Box2D facade)
│   ├── scene/                   Scene (abstract), SceneManager (stack-based FSM)
│   └── sound/                   SoundManager, SoundEffect
│
└── demo/            ◄── GAME LAYER (concrete implementation, uses engine)
    ├── StartScene.java          Title / main menu
    ├── GameScene.java           Main gameplay scene
    ├── PauseScene.java          Transparent pause overlay
    ├── Keyboard.java            Concrete InputDevice + ActionInput
    ├── GameCollisionHandler.java  ICollisionListener implementation
    ├── FrictionMovement.java    Concrete MovementModel (strategy)
    ├── MotionTuning.java        Immutable configuration value object
    ├── MotionZone.java          Sensor Shape with physics body
    └── ExplosionParticle.java   Visual particle with IExpirable auto-cleanup
```

### 2.2 Hard Boundary Rules

| Rule | Description |
|------|-------------|
| **E-1** | **Engine code must NEVER import from `demo/` (or any game package).** No class, interface, enum, or constant from the game layer may appear in any engine file. |
| **E-2** | **Engine classes must be 100 % context-free.** They must not reference car names, lane counts, traffic rules, player scores, or any domain-specific concept. |
| **E-3** | **Game code depends on Engine — never the reverse.** The dependency arrow is strictly one-way: `demo → engine`. |
| **E-4** | **The engine must be transplantable.** A developer must be able to copy the entire `engine/` package into a different LibGDX project (e.g., a platformer, a tower-defence game) and use it without modification. |
| **E-5** | **New engine features require abstraction.** If a gameplay need demands new engine capability, add it as an abstract interface or base class in `engine/`, then implement the concrete version in `demo/`. |

### 2.3 Composition Root

The **launcher** (`Lwjgl3Launcher`) is the single composition root — the only file that
knows both concrete game types and engine types. It wires:

```java
new GameMaster(new StartScene(), new Keyboard(), new SoundManager())
```

`GameMaster` accepts its initial scene, input device, and sound device via **constructor
injection**, never instantiating concrete game classes itself.

---

## 3. SOLID Principles — Mandatory Compliance

Every class and interface **must** satisfy all five SOLID principles. The AI must verify
each principle during code generation and review.

### 3.1 Single Responsibility Principle (SRP)

- Each class has **one reason to change**.
- Managers manage one concern: `EntityManager` manages entity lifecycles, `MovementManager`
  handles movement updates, `SceneManager` handles scene transitions.
- **Example violation:** A Scene class that directly instantiates physics bodies AND handles
  rendering AND processes collision responses — split into the Scene (orchestrator), a
  collision handler, and use the existing `PhysicsWorld` API.

### 3.2 Open/Closed Principle (OCP)

- Classes are **open for extension, closed for modification**.
- New movement styles → implement `MovementModel` (no changes to `MovementManager`).
- New collision responses → implement `ICollisionListener` (no changes to `CollisionManager`).
- New scenes → extend `Scene` (no changes to `SceneManager`).
- New entity types → extend `Entity`, `TextureObject`, or `Shape` (no changes to `EntityManager`).

### 3.3 Liskov Substitution Principle (LSP)

- Any subclass must be safely substitutable for its parent.
- `Entity.draw(SpriteBatch)` and `Entity.update(float)` are non-abstract with no-op
  defaults so that entities not needing them aren't forced into meaningless overrides.
- `Scene.handleInput(float)` defaults to no-op — menu scenes that don't poll input remain
  valid Scene subtypes.

### 3.4 Interface Segregation Principle (ISP)

- Interfaces must be small and focused.
- `IMovable` — only `move(float deltaTime)`.
- `IExpirable` — only `isExpired()`.
- `ICollisionListener` — collision callbacks only.
- `ActionInput` — only `isHeld(String)` and `justPressed(String)`.
- `InputDevice` — device polling and binding management only.
- `ControlSource` — axis values and action state only.
- `SoundDevice` — sound playback operations only.
- **Never combine unrelated capabilities into a single interface.**

### 3.5 Dependency Inversion Principle (DIP)

- High-level modules depend on **abstractions**, not concrete classes.
- `GameMaster` depends on `Scene`, `InputDevice`, `SoundDevice` — all interfaces/abstract
  types. It never sees `StartScene`, `Keyboard`, or `SoundManager`.
- `CollisionManager` delegates to `ICollisionListener` — it never sees `GameCollisionHandler`.
- `MovableEntity` depends on `ControlSource` and `MovementModel` — it never sees
  `UserControlled`, `AIControlled`, or `FrictionMovement`.

---

## 4. Required Design Patterns

The project **must** implement and maintain at least the following design patterns. Each
pattern has a designated location in the architecture. Do not remove or replace these; new
patterns may be added where appropriate.

### 4.1 Strategy Pattern

| Where | Interface | Concrete Implementations |
|-------|-----------|--------------------------|
| Movement feel | `MovementModel` | `FrictionMovement` (game layer) — extensible to `DriftMovement`, `IceMovement`, etc. |
| Input source | `ControlSource` | `UserControlled`, `AIControlled` — extensible to `NetworkControlled`, `ReplayControlled`, etc. |
| Collision response | `ICollisionListener` | `GameCollisionHandler` — swap for different game rules without touching the engine. |

**Rule:** When behaviour varies or needs to be swappable at runtime, use Strategy. Never
use `if/else` chains or `instanceof` cascades to select behaviour.

### 4.2 Observer Pattern

| Where | Subject | Observer |
|-------|---------|----------|
| Physics collisions | `CollisionManager` (Box2D ContactListener) | `ICollisionListener` implementations |

`CollisionManager` detects collision events via Box2D and **notifies** the registered
`ICollisionListener`. The engine never knows what the game does in response (explosions,
score changes, sound effects).

**Rule:** Game-specific reactions to engine events must always go through observer/listener
interfaces. Engine code must never call game code directly.

### 4.3 State Pattern (Stack-Based Finite State Machine)

| Where | Context | States |
|-------|---------|--------|
| Scene management | `SceneManager` (stack) | `StartScene`, `GameScene`, `PauseScene`, and any future scenes |

- `push()` — overlay a new state (e.g., pause menu on top of gameplay).
- `pop()` — return to previous state.
- `set()` — replace entire stack (e.g., main menu → gameplay).
- Only the **top** scene receives `update()` and `handleInput()` calls (Input Focus Rule).
- Transparent scenes allow the scene below to render (overlay pattern).

**Rule:** Scene transitions must always go through `SceneManager`. Scenes must never
manage other scenes directly or hold references to sibling scenes.

### 4.4 Flyweight Pattern

| Where | Shared Resource | Cache |
|-------|-----------------|-------|
| Texture management | `Texture` objects | `TextureObject.textureCache` (static `HashMap`) |

Multiple entities sharing the same sprite (e.g., 50 traffic cars using `"car_red.png"`)
reuse a single GPU `Texture` instance via `computeIfAbsent`. Individual `dispose()` is a
no-op; the cache is cleared once at shutdown via `TextureObject.disposeAllTextures()`.

**Rule:** Never call `new Texture(filename)` directly. Always go through `TextureObject`
(or extend it) to benefit from the shared cache.

### 4.5 Facade Pattern

| Where | Facade | Wrapped Complexity |
|-------|--------|--------------------|
| Physics | `PhysicsWorld` + `PhysicsBody` | Box2D `World`, `Body`, `BodyDef`, `FixtureDef`, `PolygonShape` |
| I/O | `IOManager` | Multiple `InputDevice` instances + `SoundDevice` |
| Sound | `SoundManager` | Individual `SoundEffect` instances (loop state, mute toggle) |

**Rule:** Game code must never interact with Box2D types directly. All physics operations
go through `PhysicsWorld` and `PhysicsBody`.

### 4.6 Factory Method Pattern

| Where | Factory | Product |
|-------|---------|---------|
| Viewport creation | `Scene.createViewport(OrthographicCamera)` | `FitViewport` (default) or `ExtendViewport` (gameplay override) |
| Particle spawning | `ExplosionParticle.spawnExplosion(...)` | Batch of `ExplosionParticle` instances |

**Rule:** Object creation that involves configuration logic or conditional construction
should be encapsulated in factory methods, not scattered across calling code.

---

## 5. Core OOP Concepts — Required Usage

### 5.1 Inheritance Hierarchy

```
Entity (abstract)
├── TextureObject (abstract) — sprite-based entities with Flyweight texture cache
│   ├── MovableEntity          — physics-driven, has ControlSource + MovementModel
│   └── ExplosionParticle      — self-removing visual effect (IExpirable)
└── Shape (abstract)           — ShapeRenderer-based entities (e.g., debug zones)
    └── MotionZone             — sensor area with physics body
```

**Rules:**
- Extend `TextureObject` for any entity that renders a sprite.
- Extend `Shape` for any entity that renders via `ShapeRenderer`.
- Implement `IExpirable` on any entity that should auto-remove after a condition.
- Implement `IMovable` on any entity managed by `MovementManager`.

### 5.2 Polymorphism

- `EntityManager.update()` iterates `Entity` references and calls `update(deltaTime)` —
  concrete type determines behaviour.
- `MovementManager.update()` checks `instanceof IMovable` and calls `move(deltaTime)`.
- `CollisionManager` callbacks pass `Entity` — game code uses pattern matching
  (`instanceof` with binding) to determine concrete types.

### 5.3 Encapsulation

- All fields are `private`. Access is via getters/setters.
- `Scene` exposes managers to subclasses via `protected` getters/setters only.
- `SceneManager` injection methods (`setSceneManager`, `setIOManager`) are
  **package-private** — only `SceneManager` (same package) can call them.
- `PhysicsBody` hides the raw Box2D `Body` entirely.

### 5.4 Abstraction

- Engine classes depend on interfaces (`InputDevice`, `SoundDevice`, `ICollisionListener`,
  `MovementModel`, `ControlSource`), never on concrete implementations.
- `Scene` is abstract — all scenes must provide `show()`, `update()`, `render()`.

---

## 6. Scalability Requirements

The codebase must be designed so that the following additions require **zero modification**
to existing engine code:

| Addition | How It's Achieved |
|----------|-------------------|
| **More players** | Create additional `MovableEntity` instances with different `ControlSource`/`PhysicsBody`. Add to `EntityManager`. |
| **More enemy types** | Extend `TextureObject` or `MovableEntity`. Implement custom `MovementModel` for AI behaviour. |
| **More levels / scenes** | Extend `Scene`. Push/set via `SceneManager`. |
| **More menus** | Extend `Scene`. Use `push()` for overlays, `set()` for full transitions. |
| **More input devices** | Implement `InputDevice` + `ActionInput`. Register with `IOManager.addInput()`. |
| **More movement styles** | Implement `MovementModel`. Pass to `MovableEntity` at construction. |
| **More collision rules** | Implement `ICollisionListener`. Pass to `CollisionManager` constructor. |
| **More sound effects** | Call `SoundDevice.addSound(name, path)`. Play with `playSound(name, volume)`. |
| **More surface types** | Create new `MotionTuning` presets. Assign to `MotionZone` instances. |

**Rule:** If adding a new feature requires modifying an existing engine class (other than
adding a genuinely missing abstract capability), the architecture is wrong — refactor first.

---

## 7. Low Coupling & High Cohesion

### 7.1 Coupling Rules

| Rule | Description |
|------|-------------|
| **C-1** | Engine classes must not import game-layer classes. |
| **C-2** | Managers must not hold cross-references to each other unless strictly necessary (e.g., `MovementManager` needs `EntityManager` for iteration and `PhysicsWorld` for stepping). |
| **C-3** | Scenes must not hold references to other scenes. Transitions are done by calling `SceneManager.push/pop/set` with a **new** scene instance. |
| **C-4** | Entity subclasses must not depend on specific scene types. |
| **C-5** | IOManager is the single shared I/O facade — scenes access it via `getIOManager()`, never by constructing their own. |
| **C-6** | Sound registration happens at initialization boundaries (`GameMaster.create()` for shared UI sounds, `Scene.show()` for scene-specific sounds). |

### 7.2 Cohesion Rules

| Rule | Description |
|------|-------------|
| **H-1** | Each manager handles exactly one concern. |
| **H-2** | Each interface groups only related operations. |
| **H-3** | Entity subtypes bundle only the data and behaviour relevant to their role. |
| **H-4** | Configuration is separated from behaviour (e.g., `MotionTuning` is a pure data object; `FrictionMovement` is the behaviour that uses it). |

---

## 8. Scene Sovereignty Principle

Each `Scene` is the **absolute owner** of its game world:

1. **Owns its managers:** Every Scene must create its own `EntityManager`, `MovementManager`,
   and `CollisionManager` in `show()`.
2. **Owns its entities:** Entities belong to the scene's `EntityManager`.
3. **Owns its physics world:** Each gameplay scene creates its own `PhysicsWorld`.
4. **Disposes everything:** `dispose()` must cascade to all owned managers and resources.
5. **Shares only IOManager:** The single `IOManager` instance is injected by `SceneManager`
   and is the only cross-scene shared resource.

**Why:** This prevents leaked state between scenes, ensures clean transitions, and makes
each scene independently testable.

---

## 9. Input Architecture

### 9.1 Context Stack

`Keyboard` (and any future `InputDevice`) maintains a **stack of binding contexts**:

- `pushContext()` — new scene pushes a fresh context (isolates its bindings).
- `popContext()` — scene removal restores the previous context.
- `resetToBase()` — `SceneManager.set()` clears to the base context.

This ensures pause-menu bindings don't interfere with gameplay bindings.

### 9.2 Dual Interface

`Keyboard` implements both:
- **`InputDevice`** — low-level key-to-`Runnable` command bindings (for UI: menu navigation, pause toggle).
- **`ActionInput`** — logical action polling (`isHeld("LEFT")`, `justPressed("ACTION")`) for gameplay movement.

### 9.3 ControlSource Abstraction

Movement code depends on `ControlSource`, not on `Keyboard` or `ActionInput` directly:
- `UserControlled` adapts `ActionInput` → `ControlSource`.
- `AIControlled` generates synthetic input.
- Future: `NetworkControlled`, `ReplayControlled`, etc.

---

## 10. Physics Architecture

- **`PhysicsWorld`** wraps Box2D `World`, provides a clean `createBody(...)` API.
- **`PhysicsBody`** wraps Box2D `Body`, exposes only the operations needed (velocity,
  position, impulse, damping, user data).
- **`CollisionManager`** implements Box2D `ContactListener`, extracts entity pairs, and
  delegates to `ICollisionListener`.
- **Coordinate system:** Physics operates in **metres** (`pixels / Constants.PPM`).
  Rendering operates in **pixels**. Conversion happens in `MovableEntity.syncPosition()`.

**Rules:**
- Game code must never touch `com.badlogic.gdx.physics.box2d.*` types directly.
- All body creation goes through `PhysicsWorld.createBody(...)`.
- All force/impulse application goes through `PhysicsBody` methods.

---

## 11. Resource Management

| Resource | Lifecycle | Cleanup |
|----------|-----------|---------|
| Textures | Cached in `TextureObject.textureCache` (static, app-wide) | `TextureObject.disposeAllTextures()` in `GameMaster.dispose()` |
| SpriteBatch | Created in `GameMaster.create()` | Disposed in `GameMaster.dispose()` |
| BitmapFont | Created per-scene in `show()` | Disposed per-scene in `dispose()` |
| ShapeRenderer | Created per-scene in `show()` | Disposed per-scene in `dispose()` |
| SoundEffect | Created via `SoundManager.addSound()` | `SoundManager.dispose()` via `IOManager.dispose()` |
| Music | Created per-scene in `show()` | Stopped and disposed per-scene in `dispose()` |
| PhysicsWorld (Box2D) | Created per-scene in `show()` | Disposed per-scene in `dispose()` |
| Entities | Managed by `EntityManager` per-scene | `EntityManager.dispose()` cascades to all entities |

**Rules:**
- Every `create`/`new` of a disposable resource must have a corresponding `dispose()`.
- `IExpirable` entities are auto-removed and disposed by `EntityManager.update()`.
- Never dispose shared textures individually — only via the cache at shutdown.

---

## 12. Code Style & Conventions

| Convention | Rule |
|------------|------|
| **Package naming** | `io.github.raesleg.engine.*` for engine, `io.github.raesleg.demo.*` for game |
| **Class naming** | PascalCase. Interfaces prefixed with `I` only for listener/callback types (`ICollisionListener`, `IMovable`, `IExpirable`) |
| **Method naming** | camelCase. Getters: `getX()`. Booleans: `isExpired()`, `isMuted()` |
| **Constants** | `UPPER_SNAKE_CASE` in a dedicated utility class (`Constants`) or as `static final` fields |
| **Access modifiers** | Always use the most restrictive possible. `private` by default, `protected` for subclass access, package-private for framework injection, `public` for API |
| **Immutability** | Prefer immutable objects for configuration/data (e.g., `MotionTuning`). Use `final` on fields that don't change after construction |
| **No magic numbers** | Extract constants with meaningful names. Use `Constants.PPM` instead of `100f` |
| **No God classes** | If a class exceeds ~200 lines, evaluate whether it violates SRP |

---

## 13. Error Handling & Defensive Coding

| Rule | Description |
|------|-------------|
| **D-1** | Null-check injected dependencies at system boundaries (constructor parameters, `show()` initialisation). |
| **D-2** | Use LibGDX logging (`Gdx.app.log(tag, message)`) for lifecycle events and errors — no `System.out.println`. |
| **D-3** | Guard against concurrent modification: `EntityManager` uses a pending buffer for additions and safe iterator removal for deletions. New entity types must follow this pattern. |
| **D-4** | Copy mutable data from Box2D callbacks (`impactPoint.cpy()`) to prevent use-after-free. |
| **D-5** | Physics sensor collisions are filtered in `postSolve` to avoid spurious impact events. |
| **D-6** | `Keyboard.popContext()` preserves the base context — can never pop below the root. |

---

## 14. AI Code Generation Rules

When generating or reviewing code for this project, the AI **must**:

1. **Verify engine purity.** Before writing any engine class, confirm it imports nothing
   from `demo/` and references no game-specific concept.
2. **Check SOLID compliance.** For every class: state its single responsibility, confirm it
   can be extended without modification, verify interface segregation, and ensure
   dependencies point toward abstractions.
3. **Identify the design pattern.** If new code fits a pattern slot (Strategy, Observer,
   State, Flyweight, Facade, Factory), implement it using the established interfaces.
4. **Respect Scene Sovereignty.** Never create global/static mutable state that leaks
   between scenes. Each scene owns its world.
5. **Maintain scalability.** New features must be additive (new classes), not modifying
   (changed engine classes).
6. **Follow the resource lifecycle.** Every disposable resource created in `show()` must be
   disposed in `dispose()`.
7. **Use the physics facade.** Never import `com.badlogic.gdx.physics.box2d.*` in game
   code — use `PhysicsWorld` and `PhysicsBody`.
8. **Produce minimal, focused code.** No unnecessary abstractions, no speculative features,
   no dead code.

---

## 15. Quick Reference — Where to Put New Code

| I want to add...                     | Package       | Extends / Implements                    |
|--------------------------------------|---------------|-----------------------------------------|
| A new game entity (sprite)           | `demo/`       | `TextureObject` (+ `IMovable`/`IExpirable` as needed) |
| A new game entity (shape)            | `demo/`       | `Shape`                                 |
| A new movement style                 | `demo/`       | `MovementModel`                         |
| A new AI behaviour                   | `demo/`       | `ControlSource`                         |
| A new scene / level / menu           | `demo/`       | `Scene`                                 |
| A new collision rule set             | `demo/`       | `ICollisionListener`                    |
| A new input device (gamepad, touch)  | `demo/`       | `InputDevice` + `ActionInput`           |
| A new engine capability (abstract)   | `engine/`     | New interface or abstract class          |
| A new configuration preset           | `demo/`       | Value object (like `MotionTuning`)       |

---

*This document is the single source of truth for all architectural and coding decisions.
All team members and AI tools must follow these rules without exception.*
+-----------------------------------------------------------------------+
|  SCORE: 120       [S] ---------🚗------------------ [F]   WANTED: [ ] |
|                                                                       |
|                                                                       |
|                                                                       |
|                                 |   |                                 |
|                                 |   |                                 |
|                                 |🚘 |  <-- (Player Car)               |
|                                 |   |                                 |
|                                 |   |                                 |
|                                                                       |
|                                                                       |
|                                                       SPEED: 80 KM/H  |
+-----------------------------------------------------------------------+

Breakdown of the Screen Elements:
SCORE: 120 (Top Left): * A simple text Label. It updates every second the player stays alive or when they successfully dodge a hazard.

[S] ---------🚗------------------ [F] (Top Center - The "Mario" Meter): * For now, this is just a text string! You can literally have your code generate a string of dashes (-) and insert a car emoji or the letter "C" at the correct index based on the player's percentage of distance completed between Start [S] and Finish [F].

WANTED: [ ] (Top Right - Rule Breaks): * A text Label showing how close the police are to chasing you. It could start as [ ], turn to [X] when you hit one pothole, and [X] [X] when you hit a second. Once it hits 3, Level 2 triggers the police chase!

SPEED: 80 KM/H (Bottom Right): * A text Label that updates when you hold w (increases) or s (decreases).

The Center (Gameplay): * This is where your engine's Render Manager draws the road, the player's car, the traffic lights, and eventually the police car.

---

## 16. Part 2 — Design Pattern Blueprint (Concrete Game Implementation)

> **Purpose:** This section is the architectural plan for transitioning from the abstract
> engine (Part 1) to a fully playable top-down arcade game (Part 2). It identifies **five
> design patterns** that integrate naturally into the existing architecture without
> violating the Engine/Game boundary. No existing engine class is modified — every pattern
> is implemented through **new classes** or by **extending existing abstractions**.

---

### 16.1 Pattern 1 — Object Pool (Engine Abstraction + Game Concrete)

#### Problem It Solves

Level 1 and Level 2 both require continuous spawning of AI traffic cars that scroll past
the player and leave the screen. Naively calling `new MovableEntity(...)` plus
`PhysicsWorld.createBody(...)` every few seconds (and disposing them when off-screen)
creates a GC-heavy allocation/deallocation cycle. The traffic car textures are already
Flyweight-cached, but the **physics bodies and entity objects themselves** are not reused.
The same problem applies to `ExplosionParticle` — twelve particles per collision, each
allocated and GC'd immediately after expiry.

#### Where It Is Implemented

| Class / Interface          | Package    | Role                                   |
|----------------------------|------------|----------------------------------------|
| `IPoolable` (interface)    | `engine/entity/` | Marker interface: `void reset()`, `boolean isAvailable()`. Engine-level, context-free. |
| `ObjectPool<T extends Entity & IPoolable>` | `engine/entity/` | Generic pool. Pre-allocates a fixed batch of `T`; `obtain()` returns an available instance, `release(T)` resets and returns it. Never creates beyond the ceiling. |
| `TrafficCarPool`           | `demo/`    | Concrete pool. Factory method inside creates pre-configured `MovableEntity` instances (AI-controlled, with `PhysicsBody` set to inactive). `obtain()` repositions and reactivates; `release()` deactivates physics and hides off-screen. |

#### Why It Demonstrates OOP Mastery

- **OCP:** `ObjectPool` is generic and closed for modification — any new poolable entity
  type (e.g., `BulletEntity` in a different game) only needs to implement `IPoolable`.
- **ISP:** `IPoolable` is a tiny, focused interface (`reset()` + `isAvailable()`) — does
  not pollute `Entity` or `IExpirable`.
- **DIP:** `ObjectPool` depends on `IPoolable` (abstraction), never on `MovableEntity` or
  any concrete game type.
- **Engine purity:** `IPoolable` and `ObjectPool` live in `engine/entity/` with zero game
  knowledge. `TrafficCarPool` with its car textures, AI behaviour, and lane positions
  lives in `demo/`.
- **Scalability:** Adding pooled obstacles, power-ups, or projectiles requires only a new
  concrete pool in `demo/` — no engine changes.

---

### 16.2 Pattern 2 — Builder Pattern (Game Layer — Traffic Entity Construction)

#### Problem It Solves

Constructing a traffic car currently requires coordinating **six collaborating objects** in
the correct order: texture path, pixel position → physics body (via `PhysicsWorld`), a
`ControlSource` (`AIControlled`), a `MovementModel` (`FrictionMovement` with the correct
`MotionTuning`), and finally the `MovableEntity` itself. This multi-step construction is
error-prone when scattered across `initLevelData()` in each level scene — different levels
need different speeds, different AI behaviours, and different tuning presets, leading to
duplicated and divergent construction code.

#### Where It Is Implemented

| Class                      | Package    | Role                                   |
|----------------------------|------------|----------------------------------------|
| `TrafficCarBuilder`        | `demo/`    | Fluent builder. Methods: `.texture(String)`, `.lane(int)`, `.yPosition(float)`, `.tuning(MotionTuning)`, `.aiSpeed(float)`, `.build(PhysicsWorld, EntityManager)` → `MovableEntity`. Validates required fields before construction, applies sensible defaults for optional ones. |

#### Why It Demonstrates OOP Mastery

- **SRP:** Construction logic is extracted into a dedicated builder class — level scenes
  only declare *what* they want, not *how* to wire the six dependencies.
- **OCP:** New car configurations (police car, ambulance, truck) are created by chaining
  different builder calls — the builder itself never needs modification.
- **Encapsulation:** The intricate physics-body creation, PPM conversion, and
  ControlSource/MovementModel wiring are hidden behind a clean fluent API.
- **Engine/Game boundary:** The builder lives entirely in `demo/`. It *uses* engine types
  (`PhysicsWorld`, `MovableEntity`, `ControlSource`, `MovementModel`) through their public
  APIs but never modifies them.
- **Directly addresses TODOs:** Every `initLevelData()` TODO ("Spawn AI traffic cars")
  resolves to a handful of builder calls instead of 15+ lines of manual wiring.

**Example usage sketch (Level1Scene.initLevelData):**

```
TrafficCarBuilder builder = new TrafficCarBuilder();
MovableEntity traffic = builder
    .texture("car_red.png")
    .lane(2)
    .yPosition(600f)
    .tuning(MotionTuning.DEFAULT)
    .aiSpeed(3.0f)
    .build(getWorld(), getEntityManager());
```

---

### 16.3 Pattern 3 — Command Pattern (Engine Interface + Game Implementations)

#### Problem It Solves

The current `Keyboard` binds keys directly to `Runnable` lambdas. This works for simple
fire-and-forget actions (open pause, toggle mute), but Part 2 introduces actions that
need to be **undoable, queueable, or logged** — for example:

- **Rule-break tracking:** When the player hits a pothole or collides with traffic, the
  game increments `rulesBroken`. If we later add a "grace period" or "undo last penalty"
  power-up, we need reversible commands.
- **Replay system:** Recording a sequence of player commands for a post-level replay or
  ghost car requires serialisable, self-contained command objects rather than anonymous
  lambdas.
- **Input rebinding:** Commands as first-class objects make it trivial to rebind keys at
  runtime (settings menu) by swapping which command a key maps to.

#### Where It Is Implemented

| Class / Interface           | Package    | Role                                  |
|-----------------------------|------------|---------------------------------------|
| `ICommand` (interface)      | `engine/io/` | `void execute()`, `void undo()`. Engine-level, context-free. |
| `BreakRuleCommand`          | `demo/`    | Increments `rulesBroken` counter on the scene; `undo()` decrements. |
| `ActivatePoliceCommand`     | `demo/`    | Triggers the police-chase state in Level 2; `undo()` deactivates. |
| `CommandHistory`            | `engine/io/` | Stack of executed `ICommand` instances. `executeAndRecord(ICommand)`, `undoLast()`. Engine-level utility, game-agnostic. |

#### Why It Demonstrates OOP Mastery

- **SRP:** Each command encapsulates exactly one reversible action.
- **OCP:** New game actions (speed boost, shield activation) become new `ICommand`
  implementations — `CommandHistory` and `ICommand` never change.
- **DIP:** `CommandHistory` depends on `ICommand` (abstraction), never on any concrete
  game command.
- **ISP:** `ICommand` has only two methods — `execute()` and `undo()` — no bloat.
- **Engine purity:** `ICommand` and `CommandHistory` are in `engine/io/` with zero game
  references. All concrete commands live in `demo/`.
- **Addresses Level 2 TODO:** The "police chase activation" can be modelled as a
  command object that is triggered when `rulesBroken >= WANTED_THRESHOLD`, and
  potentially reversed if the game design later adds a "bribe" mechanic.

---

### 16.4 Pattern 4 — Decorator Pattern (Engine Abstraction + Game Concrete)

#### Problem It Solves

`AIControlled` currently provides a single sinusoidal steering behaviour for all AI
traffic. Part 2 needs **composable AI variations** without creating an explosive
inheritance hierarchy:

- **Lane-locked traffic:** AI that only drives straight in its lane (Y axis only, X = 0).
- **Swerving traffic:** AI that occasionally drifts between lanes (current sine wave).
- **Pursuit AI (police):** AI that steers toward the player car's position.
- **Speed-limited AI:** Any of the above but capped to a maximum axis magnitude.

Using inheritance (`LaneLockedAI extends AIControlled`, `PursuitAI extends AIControlled`,
`SpeedLimitedLaneLockedAI extends LaneLockedAI`...) would create a combinatorial explosion
of subclasses. Decorators allow **stacking behaviours at runtime**.

#### Where It Is Implemented

| Class / Interface              | Package    | Role                                  |
|--------------------------------|------------|---------------------------------------|
| `ControlSourceDecorator` (abstract) | `engine/io/` | Implements `ControlSource`, wraps another `ControlSource`. Delegates all methods to the wrapped instance by default. Engine-level, context-free. |
| `SpeedLimitDecorator`          | `demo/`    | Clamps `getX()` and `getY()` return values to a configurable maximum magnitude. |
| `PursuitDecorator`             | `demo/`    | Overrides `getX()` / `getY()` to steer toward a target `Entity`'s position. Wraps any base `ControlSource` so pursuit can be layered on top of lane-locked or swerving AI. |
| `LaneLockDecorator`            | `demo/`    | Overrides `getX()` to return `0f`, forcing the wrapped AI to stay in its lane. |

#### Why It Demonstrates OOP Mastery

- **OCP:** New AI modifiers (e.g., `DrunkenSwerveDecorator`, `FreezeDecorator`) are new
  classes — no modification to existing AI code.
- **LSP:** Every decorator is a valid `ControlSource` — `MovableEntity` cannot
  distinguish a decorated source from a plain one.
- **DIP:** `ControlSourceDecorator` depends only on the `ControlSource` interface.
  `MovableEntity` is wired to a `ControlSource` and has no knowledge of decoration.
- **Composition over inheritance:** Two or three simple decorator classes replace an
  entire combinatorial hierarchy of AI subclasses.
- **Engine purity:** The abstract decorator base lives in `engine/io/`. Concrete
  decorators with game-specific targeting logic live in `demo/`.
- **Addresses Level 2 TODO:** Police pursuit is simply:
  `new PursuitDecorator(new SpeedLimitDecorator(new AIControlled(), 6f), playerCar)`.

---

### 16.5 Pattern 5 — Template Method (Already Present — Extended for Level Completion)

#### Problem It Solves

`BaseGameScene` already uses Template Method for level initialization and per-frame
updates. However, there is a **critical missing hook**: level-completion and game-over
transitions. Both `Level1Scene.updateGame()` and `Level2Scene.updateGame()` contain
identical TODO comments for checking `progress >= 1.0` and transitioning to a results
scene. Without a template hook, each level will duplicate the same completion-check
boilerplate, violating DRY and SRP.

Additionally, Part 2 needs a **results / game-over scene** that receives a summary of the
completed level (score, time, rules broken). This data transfer between scenes must be
clean and not leak game state.

#### Where It Is Implemented

| Class / Interface              | Package    | Role                                  |
|--------------------------------|------------|---------------------------------------|
| `LevelResult` (value object)  | `demo/`    | Immutable record: `score`, `time`, `rulesBroken`, `levelName`, `completed` (boolean). Passed to results scene via constructor. |
| `ResultsScene`                 | `demo/`    | Extends `Scene`. Displays final score, time, grade. Buttons: "Retry" (`set(new Level1Scene())`), "Next Level" (`set(new Level2Scene())`), "Main Menu" (`set(new StartScene())`). |
| `BaseGameScene` (modification) | `demo/`   | Add a `protected final` method `checkLevelEnd()` called at the end of `update()`. It checks `progress >= 1.0` (win) or a new abstract hook `isGameOver()` (loss). On trigger, transitions to `ResultsScene` with a `LevelResult` snapshot. Subclasses define `isGameOver()` — Level 2 returns `true` when police catch the player; Level 1 returns `false` (no lose condition). |

#### Why It Demonstrates OOP Mastery

- **Template Method (refined):** The completion check is a **fixed algorithm step** in
  the base class that calls an **abstract hook** (`isGameOver()`) for level-specific lose
  conditions. This is textbook Template Method — the skeleton is locked, the variant
  parts are deferred to subclasses.
- **OCP:** Adding a Level 3 with a different game-over condition (e.g., fuel runs out)
  requires only overriding `isGameOver()` — no changes to `BaseGameScene` or `ResultsScene`.
- **SRP:** Level-completion logic lives in exactly one place (`BaseGameScene.checkLevelEnd`),
  not duplicated across every level scene.
- **Encapsulation:** `LevelResult` is an immutable value object — no mutable state leaks
  between scenes. `ResultsScene` receives all the data it needs via constructor injection.
- **Scene Sovereignty maintained:** `ResultsScene` creates its own Stage and managers.
  The completed level's scene is disposed normally by `SceneManager.set()`.

---

### 16.6 Implementation Priority & Dependency Order

The patterns should be implemented in this order to minimize rework:

| Priority | Pattern            | Reason                                              |
|----------|--------------------|-----------------------------------------------------|
| **1**    | Builder            | Unblocks all `initLevelData()` TODOs immediately — traffic spawning is the critical path. |
| **2**    | Decorator          | Required to differentiate AI behaviours (lane-locked vs. pursuit) before spawning traffic makes sense. |
| **3**    | Template Method ext.| Adds level-completion and game-over flow — depends on having traffic to collide with.  |
| **4**    | Object Pool        | Performance optimisation for traffic recycling — refactor spawning after it works naively. |
| **5**    | Command            | Adds undo/replay capability — lowest priority for MVP but highest SOLID demonstration value. |

---

### 16.7 Pattern Integration Map

```
                    ┌─────────────────────────────────────────────────────────────┐
                    │                     ENGINE LAYER                            │
                    │                                                             │
                    │  ┌──────────────┐  ┌─────────────┐  ┌──────────────────┐   │
                    │  │ IPoolable    │  │ ICommand    │  │ ControlSource-   │   │
                    │  │ ObjectPool   │  │ CommandHist │  │ Decorator (abs)  │   │
                    │  └──────┬───────┘  └──────┬──────┘  └────────┬─────────┘   │
                    │         │                 │                   │             │
                    ├─────────┼─────────────────┼───────────────────┼─────────────┤
                    │         │    GAME LAYER    │                   │             │
                    │         ▼                 ▼                   ▼             │
                    │  ┌──────────────┐  ┌─────────────┐  ┌──────────────────┐   │
                    │  │ TrafficCar-  │  │ BreakRule-  │  │ PursuitDecorator │   │
                    │  │ Pool         │  │ Command     │  │ LaneLockDecorator│   │
                    │  └──────────────┘  │ ActivatePC  │  │ SpeedLimitDeco   │   │
                    │                    └─────────────┘  └──────────────────┘   │
                    │  ┌──────────────┐  ┌─────────────┐  ┌──────────────────┐   │
                    │  │ TrafficCar-  │  │ LevelResult │  │ ResultsScene     │   │
                    │  │ Builder      │  │ (value obj) │  │ (extends Scene)  │   │
                    │  └──────────────┘  └─────────────┘  └──────────────────┘   │
                    └─────────────────────────────────────────────────────────────┘
```

### 16.8 Updated "Where to Put New Code" Table (Addendum to §15)

| I want to add...                          | Package    | Extends / Implements                     | Pattern    |
|-------------------------------------------|------------|------------------------------------------|------------|
| A poolable entity type                    | `demo/`    | `Entity` + `IPoolable`; concrete `ObjectPool` subclass | Object Pool |
| A complex entity with many config options | `demo/`    | New Builder class using engine types      | Builder    |
| A reversible game action                  | `demo/`    | `ICommand`                               | Command    |
| An AI behaviour modifier                  | `demo/`    | `ControlSourceDecorator`                 | Decorator  |
| A level-completion or game-over condition | `demo/`    | Override `isGameOver()` in `BaseGameScene` subclass | Template Method |
| A results / summary screen                | `demo/`    | `Scene`                                  | Template Method + State |

---

## 17. Current Implementation Status — Game Layer File Map

> **Note:** The game package is `io.github.raesleg.game.*` (not `demo/`). All rules from
> sections 1–16 still apply — only the concrete package name differs.

### 17.1 Package Structure (Game Layer)

```
io.github.raesleg.game/
├── collision/
│   └── GameCollisionHandler.java    ICollisionListener — crash debounce (1.5s), zone callbacks
├── entities/
│   ├── ExplosionOverlay.java        Large explode.png overlay (IExpirable, EXPLOSION_DELAY)
│   ├── Pedestrian.java              CrosswalkZone-linked NPC (kinematic body)
│   ├── PoliceCar.java               Chase entity — X-tracking, speed-based approach
│   ├── StopSign.java                Decorative sign near crosswalks
│   └── vehicles/
│       ├── PlayerCar.java           MovableEntity subclass — flash-on-hit, isFlashing()
│       └── npc/
│           ├── NPCCar.java          AI traffic car extending TextureObject
│           └── world/
│               └── effects/
│                   └── ExplosionParticle.java   Batch particle spawner (Factory Method)
├── factory/
│   ├── NPCCarSpawner.java          Spawn controller — lane occupation tracking, max 2 lanes
│   ├── PuddleSpawner.java          Spawn controller — coordinated with NPCCarSpawner
│   └── TreeSpawner.java            Decorative tree spawner — min Y spacing
├── io/
│   └── Keyboard.java               InputDevice + ActionInput + context stack
├── movement/
│   ├── MotionTuning.java           Immutable presets: DEFAULT, LOW_TRACTION, HIGH_FRICTION
│   └── PlayerMovementModel.java    MovementModel — tuning-aware steering (grip scaling)
├── rules/
│   ├── BreakRuleCommand.java       ICommand — increments rulesBroken
│   ├── PedestrianHitCommand.java   ICommand — triggers instantFail
│   └── RuleManager.java            Violation counter + police aggression
├── scene/
│   ├── BaseGameScene.java          Template Method — physics, HUD, audio, checkLevelEnd
│   ├── Level1Scene.java            Sunny Road — crosswalks, pedestrians, police on violations
│   ├── Level2Scene.java            Highway Chase — police from start, no BGM, rain overlay
│   ├── LevelResult.java            Immutable value object with lossReason field
│   ├── LevelSelectScene.java       Level picker menu
│   ├── PauseScene.java             Transparent overlay — volume controls (A/D)
│   ├── ResultsScene.java           Win/Lose screen — displays loss reason
│   ├── RoadRenderer.java           Road constants (ROAD_LEFT=340, ROAD_RIGHT=940, 3 lanes)
│   └── StartScene.java             Title / main menu
├── state/
│   ├── DashboardUI.java            HUD — graphical progress bar, dashboard.png, star.png
│   └── IDashboardObserver.java     Observer interface for HUD updates
└── zone/
    ├── CrosswalkZone.java          Sensor zone — linked to Pedestrian
    ├── MotionZone.java             Sensor area with MotionTuning
    └── Puddle.java                 MotionZone + LOW_TRACTION tuning
```

### 17.2 Key Mechanics

| Feature | Implementation |
|---------|---------------|
| **Crash debounce** | GameCollisionHandler — 1.5s cooldown prevents multiple crash events per collision |
| **Explosion on 3rd crash** | BaseGameScene.crashCount — only NPC traffic crashes count; explosion + delay then ResultsScene |
| **Crosswalk violations** | BreakRuleCommand("CROSSWALK_VIOLATION") — increments rulesBroken but NOT crashCount |
| **Pedestrian hit** | PedestrianHitCommand — sets instantFail with reason "Hit a pedestrian" |
| **Loss reasons** | LevelResult.lossReason — displayed on ResultsScene: "Crashed into too many vehicles", "Hit a pedestrian", "Police caught you" |
| **Police chase (L1)** | Spawns when rulesBroken >= 3; speed-based approach with aggression scaling |
| **Police chase (L2)** | Spawns immediately; player CAN outrun at max speed (speedFactor goes negative) |
| **Puddle sliding** | PlayerMovementModel scales steeringResponse by tuning.lateralGrip/DEFAULT.lateralGrip |
| **Lane coordination** | NPCCarSpawner.getOccupiedLanesNear() — PuddleSpawner checks before spawning; max 2 lanes blocked |
| **Puddle z-order** | Puddle.draw() is no-op; PuddleSpawner.render() called in renderLevelEffects (before entity pass) |
| **Score** | Only accumulates when simulatedSpeed > 0.5f (score stops when stationary) |
| **BGM** | BaseGameScene supports null getBgmPath() — Level 2 returns null (no BGM) |
| **Progress bar** | DashboardUI — white line with car.png icon moving right (graphical, not text) |
| **Volume control** | PauseScene — "Volume" menu option, A/D to adjust; SoundDevice.getMasterVolume/setMasterVolume |
| **Tree spacing** | TreeSpawner — MIN_Y_SPACING=250f prevents clustering |
| **Police siren** | Level 2 — looped sound with distance-based volume scaling |

### 17.3 Engine Layer Additions

| File | Addition |
|------|----------|
| `SoundDevice.java` | `getMasterVolume()`, `setMasterVolume(float)` |
| `SoundManager.java` | `masterVolume` field; `playSound()` scales by masterVolume |
| `CommandHistory.java` | Stack of ICommand — `executeAndRecord()`, `undoLast()` |

