package io.github.raesleg.game.entities;

/**
 * Category tag for AI perception — replaces instanceof cascades
 * in {@link io.github.raesleg.game.movement.AIPerceptionService}.
 */
public enum PerceptionCategory {
    PEDESTRIAN,
    VEHICLE,
    OBSTACLE
}
